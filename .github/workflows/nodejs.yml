name: Node CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1

    - name: npm install, build, and run
      run: |
        npm ci
        npm run build --if-present
      env:
        CI: true
